interface:
  display_name: "Planly 场景求解"
  short_description: "分析规划场景，提交求解并解释结果"
  default_prompt: "使用 $planly-solver 优先连接插件自带的 Gateway MCP 并按需通过宿主 OAuth 登录，分析我的规划场景、构建参数，确认后提交求解，并在支持 MCP Apps 的宿主中展示结果。"
